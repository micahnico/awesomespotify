import './app.svelte.css.proxy.js';
/* src/app.svelte generated by Svelte v3.38.2 */
import {
	SvelteComponent,
	attr,
	create_component,
	destroy_component,
	detach,
	element,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	space,
	transition_in,
	transition_out
} from "../_snowpack/pkg/svelte/internal.js";

import { onMount, setContext } from "../_snowpack/pkg/svelte.js";
import { writable } from "../_snowpack/pkg/svelte/store.js";
import Router from "../_snowpack/pkg/svelte-spa-router.js";
import { wrap } from "../_snowpack/pkg/svelte-spa-router/wrap.js";
import Client from "./client.js";

function create_fragment(ctx) {
	let link0;
	let t0;
	let link1;
	let t1;
	let div;
	let router;
	let current;
	router = new Router({ props: { routes: /*routes*/ ctx[0] } });

	return {
		c() {
			link0 = element("link");
			t0 = space();
			link1 = element("link");
			t1 = space();
			div = element("div");
			create_component(router.$$.fragment);
			attr(link0, "rel", "preconnect");
			attr(link0, "href", "https://fonts.gstatic.com");
			attr(link1, "href", "https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&family=Pacifico&display=swap");
			attr(link1, "rel", "stylesheet");
			attr(div, "class", "pseudo-body");
		},
		m(target, anchor) {
			insert(target, link0, anchor);
			insert(target, t0, anchor);
			insert(target, link1, anchor);
			insert(target, t1, anchor);
			insert(target, div, anchor);
			mount_component(router, div, null);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(router.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(router.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(link0);
			if (detaching) detach(t0);
			if (detaching) detach(link1);
			if (detaching) detach(t1);
			if (detaching) detach(div);
			destroy_component(router);
		}
	};
}

function instance($$self) {
	const client = writable(new Client());
	setContext("client", client);

	const routes = {
		"/": wrap({
			asyncComponent: () => import("./pages/home.svelte.js")
		}),
		"*": wrap({
			asyncComponent: () => import("./pages/not_found.svelte.js")
		})
	};

	return [routes];
}

class App extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, {});
	}
}

export default App;